interface:
  display_name: "Native Browser Driver"
  short_description: "Chrome/Edge を direct driver で操作"
  default_prompt: "Use $native-browser-usage and the bundled MCP tools to control Chrome or Edge with an explicit browser choice."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "native-browser-control"
      description: "NativeBrowserControl MCP server"
