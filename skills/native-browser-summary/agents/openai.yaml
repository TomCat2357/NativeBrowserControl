interface:
  display_name: "Native Browser Summary"
  short_description: "Read Chrome or Edge state with the direct runner"
  default_prompt: "Use the bundled direct runner to summarize the current Chrome or Edge window."
