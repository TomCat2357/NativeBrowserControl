# Task completion checklist

- No documented lint/format commands in repo.
- Tests are listed in `docs/DOCUMENTATION.md` (e.g., `tests/test_native_browser_driver_unit.py`), but no runner command is specified.
- When finishing a task: run any relevant tests manually if applicable and verify `native-browser-control`/`python -m native_browser_control.core.server` still starts.
