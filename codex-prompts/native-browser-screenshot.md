---
description: Capture a browser screenshot with NativeBrowserDriver
argument-hint: browser=chrome|edge [window_index=0|launch=true] [full=true] [fmt=PNG]
---

Use the `native-browser-screenshot` repository skill in the current repo with these arguments:

$ARGUMENTS

Do not assume a default browser.
