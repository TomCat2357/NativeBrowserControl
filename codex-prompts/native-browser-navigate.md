---
description: Navigate the current Chrome or Edge tab with NativeBrowserDriver
argument-hint: browser=chrome|edge [window_index=0|launch=true] url=https://example.com [wait_seconds=2]
---

Use the `native-browser-navigate` repository skill in the current repo with these arguments:

$ARGUMENTS

Do not assume a default browser.
